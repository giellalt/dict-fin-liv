theme: jekyll-theme-giellalt
title: Dictionary for Finnish ⇒ Livonian
description: An electronic dictionary from Finnish to Livonian

defaults:
  - scope: { path: "" }
    values:
      layout: default

markdown: kramdown
kramdown:
  syntax_highlighter_opts:
    disable: true
